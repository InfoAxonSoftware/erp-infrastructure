# Odoo is intentionally not routed through public hostnames right now.
# The temporary demo endpoint is direct server IP access on port 8069.
# Keep this placeholder so the old template path does not recreate wildcard
# routing when the nginx container starts.
